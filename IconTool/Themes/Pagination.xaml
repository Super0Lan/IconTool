<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:IconTool">
    <Style TargetType="Button" x:Key="PaginationButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                   
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    
    
    
    
    <ControlTemplate x:Key="DefaultTemplate" TargetType="local:Pagination">
        <Border x:Name="Bd" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="1" SnapsToDevicePixels="true">
            <WrapPanel>
                <Button></Button>
                <ListBox></ListBox>
                <Button></Button>
            </WrapPanel>
        </Border>
    </ControlTemplate>
    
    
    <Style TargetType="local:Pagination">
        <Setter Property="Template" Value="{StaticResource DefaultTemplate}"></Setter>
    </Style>
</ResourceDictionary>